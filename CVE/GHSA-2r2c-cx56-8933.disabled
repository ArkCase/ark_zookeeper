#!/bin/bash

set -euo pipefail
. /.functions

ARTIFACT_IDS=(
	org.jline:jline@HOME_LIB_DIR
)

set_or_default BASE_DIR "/app"
set_or_default HOME_DIR "${BASE_DIR}/zookeeper"
set_or_default HOME_LIB_DIR "${HOME_DIR}/lib"

export HOME_LIB_DIR
exec fix-jars "GHSA-2r2c-cx56-8933" "3:4.2.1" "${ARTIFACT_IDS[@]}"
